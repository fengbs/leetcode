/**
 * Created by lideqing@xiaomi.com on 19-4-4
 */
public class SevenFourOne {
	public int cherryPickup(int[][] grid) {
		if (grid == null || grid.length == 0 || grid[0].length == 0 ) {
			return 0;
		}
		int row = grid.length;
		int col = grid[0].length;
		boolean isUsed[][] = new boolean[row][col];
		return 0;
	}
}
